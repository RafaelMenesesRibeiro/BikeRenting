uddi.enabled=true
verbose.enabled=true
clear.stations.enabled=false

uddi.url=${uddi.url}
ws.name=${ws.name}

# ws url below is used only if uddi is disabled above
ws.url=${ws.url}

station.ws.name=${station.ws.name}

